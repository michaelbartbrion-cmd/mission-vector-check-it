// ==UserScript==
// @name         Mission Vector Check It - Work Email Reader (Discovery)
// @namespace    mission-vector-check-it-work-email
// @version      0.1.7
// @description  Read-only Outlook Web list collector for Rebel Command Work Email. Never modifies mailbox state.
// @match        https://outlook.office.com/*
// @match        https://outlook.office365.com/*
// @match        https://outlook.cloud.microsoft/*
// @grant        GM_xmlhttpRequest
// @grant        GM_getValue
// @grant        GM_setValue
// @connect      base44.app
// @run-at       document-idle
// @noframes
// ==/UserScript==

(function(){
'use strict';
if(window.top!==window.self||window.MVCI_WORK_EMAIL_READER_016)return;

const VERSION='work-email-reader-0.1.6-discovery';
const DEFAULT_ENDPOINT='https://base44.app/api/apps/6aa6b7634a031657377d4fad/functions/telemetryBridge';
const ROOT_ID='mvci-work-email-reader-v016';
const ICON_SOURCE='https://raw.githubusercontent.com/michaelbartbrion-cmd/mission-vector-check-it/main/beta/vector-ppe-helper.user.js';
const ICON_KEY='mvciPostmasterIcon_v1';
const CFG_KEY='mvciWorkEmailPairingV1';

const clean=v=>String(v==null?'':v).replace(/\s+/g,' ').trim();
const wait=ms=>new Promise(r=>setTimeout(r,ms));
const attr=(el,n)=>clean(el?.getAttribute?.(n)||'');
const visible=el=>{if(!(el instanceof Element))return false;const s=getComputedStyle(el),r=el.getBoundingClientRect();return s.display!=='none'&&s.visibility!=='hidden'&&Number(s.opacity||1)>0&&r.width>2&&r.height>2&&r.bottom>0&&r.right>0&&r.top<innerHeight&&r.left<innerWidth};
const hash=s=>{let h=2166136261;for(let i=0;i<s.length;i++){h^=s.charCodeAt(i);h=Math.imul(h,16777619)}return(h>>>0).toString(36)};
const isoMaybe=v=>{const t=Date.parse(clean(v));return Number.isFinite(t)?new Date(t).toISOString():''};
const timeLike=s=>/^(?:(?:Mon|Tue|Wed|Thu|Fri|Sat|Sun)\s+)?(?:\d{1,2}:\d{2}\s*(?:AM|PM)|(?:Mon|Tue|Wed|Thu|Fri|Sat|Sun)\s+\d{1,2}\/\d{1,2}|\d{1,2}\/\d{1,2}\/\d{2,4})$/i.test(clean(s));
const symbolOnly=s=>{const x=clean(s);return !x||/^[^A-Za-z0-9]+$/.test(x)};
const initialsOf=s=>clean(s).split(/\s+/).map(w=>(w.match(/[A-Za-z0-9]/)||[''])[0]).join('').toUpperCase();
const edgeInitialsOf=s=>{const p=clean(s).split(/\s+/).filter(Boolean);if(!p.length)return'';const first=(p[0].match(/[A-Za-z0-9]/)||[''])[0],last=(p[p.length-1].match(/[A-Za-z0-9]/)||[''])[0];return`${first}${last}`.toUpperCase()};
const initialBadge=(badge,next)=>{const b=clean(badge).toUpperCase(),n=clean(next);if(!/^[A-Z0-9]{1,3}$/.test(b)||n.length<=b.length)return false;const all=initialsOf(n),edge=edgeInitialsOf(n),first=(n.match(/[A-Za-z0-9]/)||[''])[0].toUpperCase();return all===b||edge===b||(b.length===1&&first===b)};

function cfg(){return GM_getValue(CFG_KEY,{endpoint:DEFAULT_ENDPOINT,deviceId:'',token:''})||{endpoint:DEFAULT_ENDPOINT,deviceId:'',token:''}}
function saveCfg(x){GM_setValue(CFG_KEY,x)}
function paired(){const c=cfg();return!!(clean(c.deviceId)&&clean(c.token))}
function request(payload){const c=cfg();return new Promise((resolve,reject)=>{if(!c.deviceId||!c.token)return reject(new Error('Pairing not configured.'));GM_xmlhttpRequest({method:'POST',url:c.endpoint||DEFAULT_ENDPOINT,headers:{Authorization:`Bearer ${c.token}`,'X-Rebel-Device-ID':c.deviceId,'Content-Type':'application/json'},data:JSON.stringify(payload),timeout:25000,onload:r=>{let b={};try{b=JSON.parse(r.responseText||'{}')}catch(_){}if(r.status>=200&&r.status<300&&b?.ok!==false)resolve(b);else reject(new Error(clean(b?.error||`HTTP ${r.status}`)))},onerror:()=>reject(new Error('Network error')),ontimeout:()=>reject(new Error('Network timeout'))})})}

function candidateRows(){
  const found=[...document.querySelectorAll('[role="option"],[data-convid],[data-itemid],[data-item-id]')].filter(visible);
  const out=[],seen=new Set();
  for(const el of found){
    if(seen.has(el))continue;
    seen.add(el);
    const txt=clean(el.innerText||el.textContent||''),aria=attr(el,'aria-label');
    if((txt||aria).length<8)continue;
    const inMail=!!el.closest('[role="listbox"],[role="grid"],[aria-label*="message" i],[aria-label*="mail" i]');
    const hasId=!!(attr(el,'data-convid')||attr(el,'data-itemid')||attr(el,'data-item-id'));
    if(!inMail&&!hasId)continue;
    out.push(el);
  }
  return out.slice(0,150);
}

function displayedReceived(lines,timeEl){
  const explicit=clean(attr(timeEl,'aria-label')||attr(timeEl,'title')||timeEl?.textContent||'');
  if(explicit)return explicit.slice(0,160);
  const fromLines=lines.find(timeLike);
  return clean(fromLines).slice(0,160);
}

function parseRow(el,index){
  const raw=(el.innerText||el.textContent||'').trim(),text=clean(raw),aria=attr(el,'aria-label'),title=attr(el,'title');
  const dataId=attr(el,'data-itemid')||attr(el,'data-item-id')||attr(el,'data-convid')||attr(el,'data-conversation-id')||attr(el,'id');
  const lines=raw.split(/\n+/).map(clean).filter(Boolean);
  const meaningful=lines.filter(x=>!symbolOnly(x));
  const content=meaningful.filter(x=>!timeLike(x));
  while(content.length>=2&&initialBadge(content[0],content[1]))content.shift();
  const sender=content[0]||'';
  const subject=content[1]||'';
  const preview=content.slice(2,6).join(' ');
  const timeEl=el.querySelector('time,[datetime]');
  const dt=attr(timeEl,'datetime');
  const receivedAt=isoMaybe(dt);
  const displayedReceivedText=displayedReceived(lines,timeEl);
  const unread=/\bunread\b/i.test(`${aria} ${el.className||''}`);
  const flagged=/\bflagged\b/i.test(`${aria} ${title}`)||!!el.querySelector('[aria-label*="flag" i][aria-pressed="true"],[title*="flagged" i]');
  const attachments=!!el.querySelector('[aria-label*="attachment" i],[title*="attachment" i],svg[data-icon-name*="Attach" i]')||/\battachment\b/i.test(aria);
  const rawKey=dataId||hash(`${sender}|${subject}|${aria}|${text.slice(0,500)}|${index}`);
  return{
    messageKey:`outlook:${rawKey}`,
    senderName:sender,
    subject,
    previewText:preview||text.slice(0,1800),
    receivedAt:receivedAt||undefined,
    displayedReceivedText:displayedReceivedText||undefined,
    listPosition:index,
    isUnread:!!unread,
    isFlagged:!!flagged,
    hasAttachments:!!attachments,
    webUrl:location.href,
    captureLevel:'list',
    _diag:{tag:el.tagName,id:attr(el,'id'),role:attr(el,'role'),aria:aria.slice(0,500),classes:clean(el.className).slice(0,500),dataId:dataId.slice(0,300),rawLines:lines.slice(0,10),meaningful:meaningful.slice(0,10),content:content.slice(0,10),displayedReceivedText}
  };
}

function folderName(){
  const candidates=[...document.querySelectorAll('[aria-current="page"],[aria-selected="true"]')].filter(visible).map(x=>clean(attr(x,'aria-label')||x.textContent||''));
  const exact=candidates.find(x=>/^inbox$/i.test(x)||/^sent items$/i.test(x)||/^drafts$/i.test(x)||/^deleted items$/i.test(x)||/^archive$/i.test(x)||/^junk email$/i.test(x));
  if(exact)return exact;
  const loose=candidates.find(x=>/inbox/i.test(x));
  return loose?'Inbox':'Outlook Web';
}
function mailboxHint(){const a=[...document.querySelectorAll('[aria-label*="account" i],[aria-label*="profile" i]')].filter(visible).map(x=>attr(x,'aria-label')).find(Boolean);return(a||document.title||'').slice(0,220)}

function assess(messages){
  const checks=messages.map(m=>({strongId:!!clean(m?._diag?.dataId),sender:clean(m?.senderName),subject:clean(m?.subject),position:Number(m?.listPosition)}));
  const strongIdRows=checks.filter(x=>x.strongId).length;
  const validRows=checks.filter(x=>x.strongId&&x.sender.length>=2&&x.subject.length>=1&&!symbolOnly(x.sender)&&!timeLike(x.sender)&&!timeLike(x.subject)).length;
  const positionsValid=checks.every((x,i)=>Number.isFinite(x.position)&&x.position===i);
  const quality=messages.length>0&&strongIdRows===messages.length&&validRows===messages.length&&positionsValid?'good':'partial';
  return{quality,total:messages.length,strongIdRows,validRows,positionsValid};
}

function snapshot(){
  const rows=candidateRows(),messages=rows.map(parseRow),assessment=assess(messages);
  return{rows,messages,assessment,folder:folderName(),mailbox:mailboxHint(),summary:{url:location.href,title:document.title,visibleCandidates:rows.length,quality:assessment.quality,strongIdRows:assessment.strongIdRows,validRows:assessment.validRows,positionsValid:assessment.positionsValid,displayedTimeLabels:messages.filter(m=>m.displayedReceivedText).length,listboxes:document.querySelectorAll('[role="listbox"]').length,grids:document.querySelectorAll('[role="grid"]').length,firstThree:messages.slice(0,3).map(m=>({position:m.listPosition,sender:m.senderName,subject:m.subject,displayedReceivedText:m.displayedReceivedText||'',preview:m.previewText.slice(0,120)}))}};
}

async function stableSnapshot(timeout=2500){
  const started=Date.now();let best=snapshot(),last='',stable=0;
  while(Date.now()-started<timeout){
    const snap=snapshot();
    if(snap.messages.length>best.messages.length)best=snap;
    const sig=snap.messages.map(m=>`${m.listPosition}:${m.messageKey}`).join('|');
    if(sig&&sig===last)stable++;else stable=0;
    last=sig;
    if(stable>=2)return snap;
    await wait(250);
  }
  return best;
}

let state={open:false,busy:false,msg:'Discovery mode: nothing is uploaded until you press Capture visible list.',last:null};
let logoSrc='';
async function loadLogo(){
  try{const cached=localStorage.getItem(ICON_KEY)||'';if(/^data:image\/png;base64,[A-Za-z0-9+/=]+$/.test(cached))return cached}catch(_){}
  try{const r=await fetch(`${ICON_SOURCE}?t=${Date.now()}`,{cache:'no-store',credentials:'omit'});if(!r.ok)return'';const text=await r.text(),match=text.match(/data:image\/png;base64,[A-Za-z0-9+/=]+/);if(match){try{localStorage.setItem(ICON_KEY,match[0])}catch(_){}return match[0]}}catch(_){}
  return'';
}

function add(parent,tag,text,cls){const el=document.createElement(tag);if(text!=null)el.textContent=text;if(cls)el.className=cls;parent.appendChild(el);return el}
function style(){if(document.getElementById(`${ROOT_ID}-style`))return;const s=document.createElement('style');s.id=`${ROOT_ID}-style`;s.textContent=`#${ROOT_ID}{position:fixed;right:16px;bottom:16px;z-index:2147483647;font-family:Arial,sans-serif;color:#263746}#${ROOT_ID} *{box-sizing:border-box}#${ROOT_ID} .orb{width:54px;height:54px;display:flex;align-items:center;justify-content:center;margin-left:auto;padding:0;overflow:hidden;border-radius:50%;border:2px solid #204f6d;background:#fff;box-shadow:0 6px 20px #0004;font-weight:900;cursor:pointer}#${ROOT_ID} .orb img{width:38px;height:38px;object-fit:contain;display:block}#${ROOT_ID} .p{width:390px;max-width:calc(100vw - 24px);margin-bottom:8px;background:#f6f8f9;border:1px solid #cbd8df;border-radius:10px;box-shadow:0 10px 30px #0005;overflow:hidden}#${ROOT_ID} .h{background:#173e63;color:#fff;padding:12px 14px;display:flex;justify-content:space-between;gap:10px}#${ROOT_ID} .b{padding:12px}#${ROOT_ID} button.a{width:100%;padding:10px;border-radius:8px;border:1px solid #173e63;background:#173e63;color:#fff;font-weight:700;cursor:pointer;margin-top:8px}#${ROOT_ID} button.s{padding:8px;border-radius:8px;border:1px solid #cbd8df;background:#fff;cursor:pointer}#${ROOT_ID} .m{margin-top:8px;padding:8px;border:1px solid #dce5ea;border-radius:8px;background:#fff;font-size:11px;line-height:1.4}#${ROOT_ID} .warn{margin-top:8px;padding:8px;border:1px solid #f0c36b;border-radius:8px;background:#fff8e7;font-size:10px;line-height:1.35}#${ROOT_ID} pre{font-size:9px;white-space:pre-wrap;max-height:180px;overflow:auto}`;document.documentElement.appendChild(s)}
function render(){style();let r=document.getElementById(ROOT_ID);if(!r){r=document.createElement('div');r.id=ROOT_ID;document.documentElement.appendChild(r)}while(r.firstChild)r.removeChild(r.firstChild);const snap=state.last;if(state.open){const p=add(r,'div',null,'p'),h=add(p,'div',null,'h'),title=add(h,'div');add(title,'b','REBEL POSTMASTER');const sub=add(title,'div',`${VERSION} · read-only discovery`);sub.style.fontSize='10px';sub.style.opacity='.75';const close=add(h,'button','−','s');close.addEventListener('click',()=>{state.open=false;render()});const b=add(p,'div',null,'b');const info=add(b,'div');info.style.fontSize='11px';info.textContent=`Pairing: ${paired()?'configured':'needed'} · Visible candidates: ${snap?.messages?.length??'not scanned'}${snap?.assessment?` · ${snap.assessment.quality.toUpperCase()}`:''}`;const scan=add(b,'button','SCAN VISIBLE LIST · NO UPLOAD','a');scan.addEventListener('click',async()=>{state.busy=true;render();state.last=await stableSnapshot();state.busy=false;state.msg=`Discovery stabilized on ${state.last.messages.length} candidate rows. Structural quality: ${state.last.assessment.quality.toUpperCase()}. Nothing uploaded.`;render()});const send=add(b,'button','CAPTURE VISIBLE LIST','a');send.disabled=state.busy||!paired();send.addEventListener('click',capture);const config=add(b,'button','Configure pairing','s');config.style.marginTop='8px';config.addEventListener('click',configure);add(b,'div',state.msg,'m');add(b,'div','Safety: This reader never clicks a message, never sends/replies, never deletes/archives/moves, and never changes read/flag state. It captures list text only. Full message bodies are not implemented. Exact timestamps are stored only when Outlook exposes a machine-readable datetime; otherwise the displayed time/date label is retained as text without inference.','warn');if(snap){const d=add(b,'details');d.style.marginTop='8px';const sum=add(d,'summary','Discovery details');sum.style.fontSize='11px';sum.style.cursor='pointer';add(d,'pre',JSON.stringify(snap.summary,null,2));}}const orb=add(r,'button',null,'orb');orb.title='Rebel Postmaster';orb.setAttribute('aria-label','Toggle Rebel Postmaster');if(logoSrc){const img=add(orb,'img');img.src=logoSrc;img.alt='';img.setAttribute('aria-hidden','true')}else orb.textContent='MAIL';orb.addEventListener('click',()=>{state.open=!state.open;render()})}
function configure(){const old=cfg();const endpoint=prompt('Rebel Command telemetry endpoint',old.endpoint||DEFAULT_ENDPOINT);if(endpoint==null)return;const deviceId=prompt('Rebel Command device ID',old.deviceId||'');if(deviceId==null)return;const token=prompt('Private device token (stored only in Tampermonkey userscript storage)',old.token||'');if(token==null)return;saveCfg({endpoint:clean(endpoint)||DEFAULT_ENDPOINT,deviceId:clean(deviceId),token:clean(token)});state.msg='Pairing saved in Tampermonkey storage for this Outlook reader.';render()}
async function capture(){state.busy=true;render();try{const snap=await stableSnapshot();state.last=snap;if(!snap.messages.length)throw new Error('No credible visible message rows were found. Discovery must be tuned before capture.');const capturedAt=new Date().toISOString(),captureId=`workmail:${Date.now()}:${Math.random().toString(36).slice(2,9)}`;const q=snap.assessment.quality;const payload={version:VERSION,workEmailCapture:{captureId,capturedAt,source:'Outlook Web rendered UI',sourceVersion:VERSION,mailboxHint:snap.mailbox,folderName:snap.folder,visibleCount:snap.messages.length,quality:q,notes:`Discovery collector 0.1.6. Stable list-only capture; ${snap.assessment.validRows}/${snap.assessment.total} structurally valid rows, ${snap.assessment.strongIdRows}/${snap.assessment.total} strong Outlook DOM IDs, and visible ordering preserved. No mailbox mutations performed.`,messages:snap.messages.map(({_diag,...m})=>m)}};const res=await request(payload);if(!res?.workEmail?.accepted)throw new Error(res?.workEmail?.error||res?.workEmail?.reason||'Rebel Command did not accept the capture.');state.msg=`Captured ${res.workEmail.messages??snap.messages.length} list rows to Rebel Command as ${q.toUpperCase()} evidence. No mailbox state changed.`}catch(e){state.msg=`STOPPED: ${clean(e?.message||e)} No mailbox changes were made.`}finally{state.busy=false;render()}}

state.last=snapshot();
render();
loadLogo().then(src=>{if(src){logoSrc=src;render()}}).catch(()=>{});
window.MVCI_WORK_EMAIL_READER_016={version:VERSION,snapshot,stableSnapshot,capture,configure,status:()=>({paired:paired(),messages:state.last?.messages?.length||0,quality:state.last?.assessment?.quality||'unknown',url:location.href})};
})();
