(function(){
'use strict';
const VERSION='0.13.0-dev';
const STATE_KEY='missionVectorScheduling_v3';
const RUN_KEY='missionVectorScheduling_backfill_v0130';
const ANCHOR_DAY1='2026-09-10';
const ANCHOR_DAY2='2026-09-11';
if(window.top!==window.self||window.__mvciVectorBackfill0130)return;
window.__mvciVectorBackfill0130={version:VERSION,startedAt:Date.now()};

const clean=v=>String(v==null?'':v).replace(/\s+/g,' ').trim();
const esc=v=>String(v==null?'':v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
function loadState(){try{return JSON.parse(localStorage.getItem(STATE_KEY)||'null');}catch(_){return null;}}
function saveState(s){if(!s)return;s.metadata=s.metadata||{};s.metadata.runtimeVersion=VERSION;s.metadata.liveLoader=!!window.__mvciLiveLoader;s.metadata.liveLoaderVersion=window.__mvciLiveLoader?.loaderVersion||null;s.metadata.runtimeLoadedAt=new Date().toISOString();s.metadata.updatedAt=new Date().toISOString();localStorage.setItem(STATE_KEY,JSON.stringify(s));}
function loadRun(){try{return JSON.parse(localStorage.getItem(RUN_KEY)||'null');}catch(_){return null;}}
function saveRun(r){if(!r)localStorage.removeItem(RUN_KEY);else localStorage.setItem(RUN_KEY,JSON.stringify(r));}
function parseDate(date){const d=new Date(`${date}T12:00:00`);return Number.isNaN(d.getTime())?null:d;}
function fmt(d){return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;}
function addDays(date,n){const d=parseDate(date);if(!d)return null;d.setDate(d.getDate()+n);return fmt(d);}
function priorDate(date){return addDays(date,-1);}
function dateToHash(date){const[y,m,d]=date.split('-');return `#${y}/${m}/${d}`;}
function reader(){const r=window.MVCI_VECTOR_READER_0120;if(!r)throw new Error('Vector reader 0.12.0 is not loaded. Refresh Vector and try again.');return r;}
function dayDiff(a,b){const A=parseDate(a),B=parseDate(b);return Math.round((A-B)/86400000);}
function mod(n,m){return((n%m)+m)%m;}
function isCShiftDate(date){const phase=mod(dayDiff(date,ANCHOR_DAY1),6);return phase===0||phase===1;}
function previousCShiftDate(date){let d=priorDate(date);for(let i=0;i<6;i++){if(isCShiftDate(d))return d;d=priorDate(d);}return null;}
function countEligible(start,end,cOnly){let n=0,d=end;while(d&&d>=start){if(!cOnly||isCShiftDate(d))n++;d=priorDate(d);}return n;}
function ensureSettings(state){state.settings=state.settings||{};if(typeof state.settings.cShiftBackfillOnly!=='boolean')state.settings.cShiftBackfillOnly=true;state.settings.cShiftAnchorDay1=ANCHOR_DAY1;state.settings.cShiftAnchorDay2=ANCHOR_DAY2;saveState(state);return state;}

function ensureBackfillRecord(state,run){state.backfillRuns=Array.isArray(state.backfillRuns)?state.backfillRuns:[];let rec=state.backfillRuns.find(x=>x.id===run.id);if(!rec){rec={id:run.id,startedAt:run.startedAt,startDate:run.startDate,startingDate:run.startingDate,status:run.status||'running',captures:run.captures||0};state.backfillRuns.push(rec);}return rec;}
function persistRun(run,extra={}){Object.assign(run,extra,{updatedAt:new Date().toISOString()});saveRun(run);const state=loadState();if(!state)return;const rec=ensureBackfillRecord(state,run);Object.assign(rec,{status:run.status,captures:run.captures||0,lastCapturedDate:run.lastCapturedDate||null,updatedAt:run.updatedAt,error:run.error||null,completedAt:run.completedAt||null,stoppedAt:run.stoppedAt||null,cShiftOnly:!!run.cShiftOnly,anchorDay1:ANCHOR_DAY1,anchorDay2:ANCHOR_DAY2});state.metadata=state.metadata||{};state.metadata.lastBackfillStatus=run.status;state.metadata.lastBackfillDate=run.lastCapturedDate||null;state.metadata.lastBackfillCaptures=run.captures||0;state.metadata.lastBackfillError=run.error||null;state.metadata.lastBackfillCShiftOnly=!!run.cShiftOnly;saveState(state);}
function compactCapturedDate(state,date){if(!Array.isArray(state?.observations))return;const tracked=new Set([...(state.settings?.firefighters||[]),...(state.settings?.command||[])].map(p=>p.id));const keep=[],latest=new Map();for(const o of state.observations){const ours=o.date===date&&tracked.has(o.personId)&&String(o.source||'').startsWith('vector-dom-readonly-v0120');if(!ours){keep.push(o);continue;}const prev=latest.get(o.personId);if(!prev||String(o.capturedAt||'')>String(prev.capturedAt||''))latest.set(o.personId,o);}for(const o of latest.values())keep.push(o);state.observations=keep;}
async function waitForVisibleDate(target,timeout=15000){const started=Date.now();let consecutive=0;while(Date.now()-started<timeout){await sleep(250);const visible=reader().visibleDisplayedDate();if(visible===target){consecutive++;if(consecutive>=2)return true;}else consecutive=0;}return false;}
function findPreviousControl(){const candidates=[];for(const doc of reader().pageDocuments()){let els=[];try{els=[...doc.querySelectorAll('button,a,[role="button"]')];}catch(_){continue;}for(const el of els){if(el.closest?.('#mvci-vs-panel,#mvci-vs-open'))continue;const label=clean(`${el.getAttribute?.('aria-label')||''} ${el.getAttribute?.('title')||''} ${el.textContent||''}`).toLowerCase();if(/previous\s*(day|date)?|prev\s*(day|date)?|prior\s*(day|date)?/.test(label))candidates.push({el,score:100});else if(/(^|\s)(‹|«|←|◀)(\s|$)/.test(label))candidates.push({el,score:20});}}candidates.sort((a,b)=>b.score-a.score);return candidates[0]?.el||null;}
async function navigateToDate(target,current){if(/\/ListView\/?$/i.test(location.pathname)||/\/ListView\//i.test(location.pathname)){location.hash=dateToHash(target);if(await waitForVisibleDate(target,15000)){await sleep(400);return target;}}if(target===priorDate(current)){const prev=findPreviousControl();if(prev){prev.click();if(await waitForVisibleDate(target,15000)){await sleep(400);return target;}}}throw new Error(`Could not navigate from ${current} to ${target}.`);}
function captureCurrent(){const out=reader().captureNow();const state=loadState();compactCapturedDate(state,out.date);saveState(state);return out;}

async function runBackfill(){let run=loadRun();if(!run?.active||window.__mvciBackfillLoop0130)return;window.__mvciBackfillLoop0130=true;try{if(reader().pageMode()!=='ListView')throw new Error('Open Vector ListView before running historical backfill.');while((run=loadRun())?.active){const visible=reader().visibleDisplayedDate();const date=visible||reader().displayedDate();if(!date)throw new Error('Could not determine the visibly displayed Vector date during backfill.');if(date<run.startDate){run.active=false;run.status='complete';run.completedAt=new Date().toISOString();persistRun(run);refreshUI();break;}if(run.cShiftOnly&&!isCShiftDate(date)){const target=previousCShiftDate(date);if(!target||target<run.startDate){run.active=false;run.status='complete';run.completedAt=new Date().toISOString();persistRun(run);refreshUI();break;}await navigateToDate(target,date);continue;}const out=captureCurrent();if(out.date!==date)throw new Error(`Reader/date mismatch: visible ${date}, capture ${out.date}. Backfill stopped rather than mis-date evidence.`);run.captures=(run.captures||0)+1;run.lastCapturedDate=date;run.lastSummary=loadState()?.metadata?.lastCaptureSummary||null;run.status='running';persistRun(run);refreshUI();const target=run.cShiftOnly?previousCShiftDate(date):priorDate(date);if(!target||target<run.startDate){run.active=false;run.status='complete';run.completedAt=new Date().toISOString();persistRun(run);refreshUI();alert(`Historical backfill complete.\n\nCaptured ${run.captures} ${run.cShiftOnly?'C-shift':'calendar'} dates through ${date}.\nExport the private state JSON and send it here for reconciliation.`);break;}await navigateToDate(target,date);}}
catch(err){run=loadRun()||{id:`error-${Date.now()}`,captures:0};run.active=false;run.status='stopped-error';run.error=String(err?.message||err);persistRun(run);refreshUI();alert(`Historical backfill stopped safely.\n\n${run.error}\n\nNo Vector data was changed.`);}finally{window.__mvciBackfillLoop0130=false;refreshUI();}}
function startBackfill(){const state=ensureSettings(loadState());if(!state)throw new Error('Scheduling state has not been initialized.');const start=state.settings?.ratioStartDate||state.ratioPeriods?.find?.(p=>!p.endDate)?.startDate;if(!start)throw new Error('Set the active ratio start date first.');const current=reader().visibleDisplayedDate()||reader().displayedDate();if(!current)throw new Error('Could not determine the displayed Vector date.');if(reader().pageMode()!=='ListView')throw new Error('Open ListView first.');if(current<start)throw new Error(`Displayed date ${current} is before active ratio start ${start}.`);const cOnly=state.settings.cShiftBackfillOnly!==false;const expected=countEligible(start,current,cOnly);const mode=cOnly?`C Shift only (${expected} expected dates; 2 on / 4 off anchored to ${ANCHOR_DAY1} and ${ANCHOR_DAY2})`:`every calendar date (${expected} expected dates)`;if(!confirm(`Backfill Truck 504 history from ${current} backward through ${start}?\n\nMode: ${mode}\n\nREAD-ONLY against Vector.`))return;const run={id:`bf-${Date.now()}`,active:true,status:'running',startedAt:new Date().toISOString(),startDate:start,startingDate:current,captures:0,lastCapturedDate:null,error:null,cShiftOnly:cOnly,anchorDay1:ANCHOR_DAY1,anchorDay2:ANCHOR_DAY2,expectedDates:expected};persistRun(run);refreshUI();setTimeout(runBackfill,150);}
function stopBackfill(){const run=loadRun();if(!run)return;run.active=false;run.status='stopped-user';run.stoppedAt=new Date().toISOString();persistRun(run);refreshUI();}
function ensureStyle(){if(document.getElementById('vs-v0130-hide-old'))return;const s=document.createElement('style');s.id='vs-v0130-hide-old';s.textContent='#vs-live-build-status-v0120,#vs-backfill-card-v0120{display:none!important}';document.documentElement.appendChild(s);}
function setCard(panel,id,html,where){let card=panel.querySelector('#'+id);if(!card){card=document.createElement('div');card.id=id;card.className='vs-card';where(card);}if(card.dataset.html!==html){card.dataset.html=html;card.innerHTML=html;}return card;}
function refreshUI(){ensureStyle();const panel=document.getElementById('mvci-vs-panel');if(!panel)return;const state=ensureSettings(loadState());if(!state)return;const run=loadRun(),loader=window.__mvciLiveLoader?.loaderVersion||state.metadata?.liveLoaderVersion||'legacy/static loader';const statusHtml=`<h3>Live build status</h3><div><b>Runtime:</b> ${VERSION}</div><div><b>Loader:</b> ${esc(loader)}</div><div><b>Reader:</b> ${esc(window.MVCI_VECTOR_READER_0120?.version||'missing')}</div><div><b>Backfill:</b> ${esc(state.metadata?.lastBackfillStatus||'not started')} · ${Number(state.metadata?.lastBackfillCaptures||0)} dates</div>`;setCard(panel,'vs-live-build-status-v0130',statusHtml,card=>{const first=panel.querySelector('.vs-card');if(first)first.insertAdjacentElement('beforebegin',card);else panel.prepend(card);});const start=state.settings?.ratioStartDate||state.ratioPeriods?.find?.(p=>!p.endDate)?.startDate||'not set';const current=reader().visibleDisplayedDate?.()||reader().displayedDate?.()||'unknown';const cOnly=state.settings.cShiftBackfillOnly!==false;const expected=(start!=='not set'&&current!=='unknown')?countEligible(start,current,cOnly):0;const progress=run?.active?`Running · ${run.lastCapturedDate||current} · ${run.captures||0}/${run.expectedDates||'?'} dates captured`:run?.status==='complete'?`Last run complete · ${run.captures||0} dates · through ${run.lastCapturedDate||''}`:run?.error?`Last run stopped: ${run.error}`:'Not running';const backfillHtml=`<h3>Historical ratio backfill <span class="vs-muted">${VERSION}</span></h3><div class="vs-muted" style="margin-bottom:7px">Ratio start ${esc(start)} · displayed ${esc(current)}</div><label style="display:flex;gap:7px;align-items:center;margin:7px 0"><input id="vs-cshift-only-v0130" type="checkbox" ${cOnly?'checked':''}><b>C Shift only</b> — 2 days on / 4 days off</label><div class="vs-muted" style="margin-bottom:7px">Anchors: ${ANCHOR_DAY1} and ${ANCHOR_DAY2} were work days. Current scan: ${expected} eligible dates.</div><div style="margin-bottom:8px">${esc(progress)}</div><div style="display:flex;gap:6px"><button id="vs-backfill-start-v0130" class="vs-btn" style="flex:1" ${run?.active?'disabled':''}>Backfill active ratio period</button><button id="vs-backfill-stop-v0130" class="vs-btn secondary" style="flex:0 0 90px" ${run?.active?'':'disabled'}>Stop</button></div><div class="vs-muted" style="margin-top:7px">Read-only. C Shift filter skips scheduled off-days but can be unchecked if you ever need a full calendar audit.</div>`;const card=setCard(panel,'vs-backfill-card-v0130',backfillHtml,c=>{const diag=panel.querySelector('#vs-capture-diagnostics-v0120');if(diag)diag.insertAdjacentElement('afterend',c);else panel.appendChild(c);});const chk=card.querySelector('#vs-cshift-only-v0130');if(chk&&!chk.dataset.wired){chk.dataset.wired='1';chk.onchange=()=>{const s=loadState();if(!s)return;s.settings=s.settings||{};s.settings.cShiftBackfillOnly=!!chk.checked;saveState(s);refreshUI();};}const startBtn=card.querySelector('#vs-backfill-start-v0130');if(startBtn&&!startBtn.dataset.wired){startBtn.dataset.wired='1';startBtn.onclick=()=>{try{startBackfill();}catch(e){alert(e.message||e);}};}const stopBtn=card.querySelector('#vs-backfill-stop-v0130');if(stopBtn&&!stopBtn.dataset.wired){stopBtn.dataset.wired='1';stopBtn.onclick=stopBackfill;}}
setInterval(refreshUI,1200);setTimeout(refreshUI,300);if(loadRun()?.active)setTimeout(runBackfill,1200);window.MVCI_VECTOR_BACKFILL_0130={version:VERSION,startBackfill,stopBackfill,runBackfill,isCShiftDate,previousCShiftDate};
})();
